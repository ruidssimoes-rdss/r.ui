# Switch

A toggle switch component with animated thumb for on/off states.

## Installation

```bash
npx r-ui add switch
```

## Usage

```tsx
import { Switch } from '@r-ui/react-native'

export default function MyComponent() {
  const [enabled, setEnabled] = useState(false)

  return (
    <Switch
      checked={enabled}
      onCheckedChange={setEnabled}
      label="Enable dark mode"
    />
  )
}
```

## Interactive Example

<ComponentPreview code={`<Switch checked={checked} onCheckedChange={setChecked} />`}>
  <SwitchBasicPreview />
</ComponentPreview>

## With Label and Description

<ComponentPreview code={`<Switch
  checked={notifications}
  onCheckedChange={setNotifications}
  label="Push Notifications"
  description="Receive push notifications on your device"
/>`}>
  <SwitchWithLabelPreview />
</ComponentPreview>

## Disabled States

<ComponentPreview code={`<Switch disabled />
<Switch checked disabled />`}>
  <SwitchDisabledPreview />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | `false` | Checked state |
| `onCheckedChange` | `(checked: boolean) => void` | - | Change handler |
| `disabled` | `boolean` | `false` | Disable the switch |
| `label` | `string` | - | Switch label |
| `description` | `string` | - | Additional description text |
| `style` | `ViewStyle` | - | Container styles |
