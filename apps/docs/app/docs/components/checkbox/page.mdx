# Checkbox

A checkbox input with animated checkmark for boolean selections.

## Installation

```bash
npx r-ui add checkbox
```

## Usage

```tsx
import { Checkbox } from '@r-ui/react-native'

export default function MyComponent() {
  const [checked, setChecked] = useState(false)

  return (
    <Checkbox
      checked={checked}
      onCheckedChange={setChecked}
      label="Accept terms and conditions"
    />
  )
}
```

## Basic Usage

<ComponentPreview code={`<Checkbox label="Enable notifications" />`}>
  <CheckboxBasicPreview />
</ComponentPreview>

## With Label

<ComponentPreview code={`<Checkbox
  label="Marketing emails"
  description="Receive updates about new features and offers"
/>`}>
  <CheckboxWithLabelPreview />
</ComponentPreview>

## Disabled States

<ComponentPreview code={`<Checkbox disabled label="Disabled unchecked" />
<Checkbox disabled checked label="Disabled checked" />`}>
  <CheckboxDisabledPreview />
</ComponentPreview>

## Checkbox Group

<ComponentPreview code={`<Checkbox label="Option 1" checked />
<Checkbox label="Option 2" />
<Checkbox label="Option 3" checked />`}>
  <div className="space-y-3">
    <div className="flex items-center gap-3">
      <div className="w-5 h-5 rounded flex items-center justify-center" style={{ backgroundColor: 'var(--checkbox-bg-checked)' }}>
        <svg className="w-3 h-3" viewBox="0 0 12 12" fill="none" stroke="var(--checkbox-check)" strokeWidth="2">
          <path d="M2 6l3 3 5-6" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>
      <span className="text-sm font-medium" style={{ color: 'var(--component-text)' }}>Option 1</span>
    </div>
    <div className="flex items-center gap-3">
      <div className="w-5 h-5 rounded bg-transparent" style={{ border: '2px solid var(--checkbox-border)' }} />
      <span className="text-sm font-medium" style={{ color: 'var(--component-text)' }}>Option 2</span>
    </div>
    <div className="flex items-center gap-3">
      <div className="w-5 h-5 rounded flex items-center justify-center" style={{ backgroundColor: 'var(--checkbox-bg-checked)' }}>
        <svg className="w-3 h-3" viewBox="0 0 12 12" fill="none" stroke="var(--checkbox-check)" strokeWidth="2">
          <path d="M2 6l3 3 5-6" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>
      <span className="text-sm font-medium" style={{ color: 'var(--component-text)' }}>Option 3</span>
    </div>
  </div>
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | `false` | Checked state |
| `onCheckedChange` | `(checked: boolean) => void` | - | Change handler |
| `disabled` | `boolean` | `false` | Disable the checkbox |
| `label` | `string` | - | Checkbox label |
| `description` | `string` | - | Additional description text |
| `style` | `ViewStyle` | - | Container styles |
