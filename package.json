{
  "name": "r-ui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @r-ui/docs dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "storybook": "pnpm --filter @r-ui/storybook start"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
